The hero screenshot for the sc-agent README will live here as `hero-bracket-overlay.png`.

Capture target: in-game with `r_DisplayInfo 3` enabled, sc-agent bracket overlay
pointing at a saved POI. ~1280x720 PNG, optimized with `oxipng -o4`. Replaces the
HTML-commented-out image directive at the top of /README.md once available.
